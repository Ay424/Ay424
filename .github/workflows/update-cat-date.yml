name: 🗓 Update Cat Image Date

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC
  workflow_dispatch:

jobs:
  update-date:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          persist-credentials: true

      - name: Update date in README
        run: |
          DATE=$(date -u +"%Y-%m-%d")
          sed -i "s|date=[0-9\-]*|date=$DATE|" README.md

      - name: Commit and push if changed
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add README.md
          git commit -m "📅 Update cat image date to $DATE" || echo "No changes to commit"
          git push
